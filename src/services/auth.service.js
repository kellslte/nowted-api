
// register the user
/* 
function to register users
payload: {
    name, email, password
}

// new User.create(payload).save().then(user => user.toObject());
// const newuser = await User.create(payload)
 await user.save();

 const folder = await Folder.create({
    user: newUser,
    folders: {}
 })

//  user creates a new folder
await Folder.findByIdAnUpdate(id, {
    folders: {
        personal: ['jskfauheiuhuhiweruhi', 'asjkufhewiusdfhueiwufhe'],
        work: ['sdgfkdglijdfugnhidu', 'jsihdauwifuhdwuehsufie']
    }
});
*/